name: Patch Release

on:
  workflow_dispatch:

env:
  NEW_VERSION: '\${parsedVersion.majorVersion}.\${parsedVersion.minorVersion}.\${parsedVersion.nextIncrementalVersion}'

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3.5.0
      - name: CD
        uses: ./.github/actions/cd.yml
